{
  "buildCommand": "npm install --legacy-peer-deps && npm run build",
  "rewrites": [
    { "source": "/game/:path*", "destination": "/game/:path*" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
