{
  "buildCommand": "npm install --legacy-peer-deps && npm run build",
  "rewrites": [
    {
      "source": "/game/(.*)",
      "destination": "/game/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
